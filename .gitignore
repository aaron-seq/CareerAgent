# Claude Code agent worktrees (isolated checkouts for background agents)
.claude/worktrees/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
.env

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Project Specific
debug_parsing.py
verify_core.py
legacy_oil_spill/
temp_cv_*.pdf
*.log

# Databases (local dev)
*.db
*.sqlite
*.sqlite3
careeragent.db

# Local encryption key material
.careeragent_key
# Downloaded enrichment datasets (large; licences restrict redistribution)
core/enrichment/data/*.csv

# Personal documents and parsed PII - never commit (see CLAUDE.md "Secrets & PII").
# These paths are ignored for new files. If any are already tracked, remove
# them from the index with `git rm -r --cached <path>` before committing.
*_C_V.pdf
*_CV.pdf
cv_*.pdf
resume_*.pdf
careeragent_data/cv_profiles/
careeragent_data/contacts/
careeragent_data/job_postings/
